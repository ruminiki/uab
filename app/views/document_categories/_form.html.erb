<%= form_for @document_category, :html => { :class => "form-horizontal document_category" } do |f| %>

    <% if @document_category.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-body">
        <ul>
        <% @document_category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nome, :class => 'col-sm-2 control-label' %>
    <div class="controls">
      <%= f.text_field :name, :autofocus => true, :class => 'form-control', :style=> 'max-width: 600px' %>
    </div>
    <%= error_span(@document_category[:name]) %>
  </div>

  <br>

  <%= f.submit "Salvar Categoria", :class => 'col-sm-offset-2 btn btn-primary' %>
  <%= link_to "Cancelar", document_categories_path, :class => 'btn btn-default' %>

<% end %>
